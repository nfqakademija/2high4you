{% extends '::base.html.twig' %}

{% block body %}
    <h1>Įveskite noriu išmokti informaciją...</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    {{ user }} </br>
    {{ adv}}
{% endblock %}